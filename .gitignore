*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# 배포용 APK를 모아두는 곳. GitHub Releases에 올려서 배포하므로 저장소에는 넣지 않는다
# (바이너리를 커밋하면 버전마다 ~9MB씩 히스토리에 영구히 쌓인다).
/dist

# 릴리즈 서명 키와 비밀번호. 절대 커밋하지 말 것.
# 이 키를 잃어버리면 이미 설치된 앱 위에 업데이트를 못 한다(서명 불일치) — 따로 백업해둘 것.
*.jks
*.keystore
keystore.properties
